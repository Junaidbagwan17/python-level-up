# function witn outputs

# create parameters f_name  and l_name
def format_name(f_name, l_name):# parametesr : f_name and l_name
    # conv parameters into title case
    print(f_name.title())
    print(l_name.title())
format_name(f_name="virAt", l_name='kOhli')

# storing into var function-title into var
def format_name(f_name, l_name):# parametesr : f_name and l_name
    # conv parameters into title case
    formated_fname = f_name.title()
    formated_lname= l_name.title()

    print(f"{formated_fname} {formated_lname}")
format_name(f_name="virAt", l_name='kohLi')

