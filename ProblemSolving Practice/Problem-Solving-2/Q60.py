# todo: Write a program that finds the largest and smallest elements in a list
from numpy.ma.core import minimum

numbers = [52, 43 , 2, 56, 85, 5, 60]

print("Maximum:",max(numbers))
print("Minimum:",min(numbers))
